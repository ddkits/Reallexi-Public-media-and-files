# Changelog

Generated by build: 2026-07-26T13:21:51.901Z

## 1.3.123 - Clean Extension Build

- Provider-neutral mutating tool intake now recovers every complete path/content or replacement edit from malformed JSON/function envelopes without guessing missing data, discards truncated edits, and gives an unrecoverable write schema one bounded correction turn instead of misreporting it as exhausted workspace inspection.
- Incomplete full-project batches now end as resumable code-review checkpoints instead of false completion/failure results: approval preserves Continue ownership, validation and UI proof remain deferred, long product specifications cannot be relabeled as generic runtime failures, internal attachment/agent wrappers cannot invent unrelated deliverables, and resumed roadmaps reuse existing approved files with case-insensitive config/persistence coverage instead of rewriting them.
- The package, browser, backend, and web dependency trees now resolve with zero npm audit findings: PostCSS, fast-uri, brace-expansion, and linkify-it are pinned to patched releases, the obsolete unused JavaScript obfuscator tree is removed, and the secured Mocha toolchain retains the packaged extension and standalone CLI Node 18 compatibility contract.
- Model selection is now authoritative across Auto, Planner + Coder, One Model, and Direct Model: NVIDIA and other provider choices survive hydration and catalog refresh, planner/action lanes remain independent in split flow, refresh recovery preserves saved choices, and every transport or catalog fallback is labeled explicitly without rewriting the saved selection.
- Full-project runs now keep incomplete implementation slices in per-file code review and defer UI screenshot proof until executable manifests, source roots, persistence, roadmap targets, and request-defined repository surfaces are complete; global runtime exports are excluded from project targets/evidence, and authoritative Unreal/Node specifications seed real foundation files instead of documentation-only roadmaps.
- Validation repair now accepts executable patch JSON carried inside provider response envelopes, so valid model-owned config/source fixes are applied instead of misclassified as malformed; consecutive identical failures without a source mutation reset on new evidence and stop at the configured no-progress threshold instead of rerunning forever.
- Verified full-context attachment missions now allow one corrective reread/no-tool turn, then hand the same authoritative specification and workspace checkpoint to the bounded DB Writer; prompt-strategy text cannot reset no-progress evidence, guessed prose filenames are never probed as files, internal attachment paths stay private, and guard exhaustion is recovered instead of shown as a final failure.
- The VS Code Agent Thinking strip stays a compact dock while collapsed and becomes a clear foreground popup only when explicitly expanded, with an isolated backdrop, independent scrolling, accessible dialog state, Escape-to-close focus restoration, responsive geometry, and compact consistent action buttons.
- Chrome and Edge service workers now retain a bounded privacy-safe structural diagnostic ring for startup, storage, router, port, and unhandled failures, with migration/corruption recovery plus explicit download and clear controls that never persist prompts, page content, URLs, hosts, credentials, cookies, or raw exception payloads.
- Autonomous happy-path recovery now accepts complete SHA/page/schema-valid long-attachment ledgers well beyond the former 8,100-character gate, rotates through up to four compatible rescue models across Auto, Planner + Coder, One Model, and Direct Model, treats stale NVIDIA model/function deployments as bounded retryable failures, preserves the saved selection, and never asks for Continue for an ordinary recoverable provider block.
- Verified long-attachment ledger repairs now treat the model-derived limit as a compact target with a bounded automatic safety ceiling: SHA/page/schema-valid ledgers may continue up to 35% above the target with explicit activity and SQLite artifact evidence, while larger or unverified responses still repair or stop safely instead of terminating a mission at the observed 7,585-versus-6,000 boundary.
- Long-attachment ledger mapping now extracts the largest complete balanced JSON object from mixed model envelopes, gives no-tools structured coder responses their configured output budget, and sends one bounded rejected candidate back to the same model for schema-only repair instead of terminating or restarting the mission.
- Standalone CLI llama.cpp chat now discovers bounded loopback servers, excludes embedding and reranker models from chat/planner routing, handles bare test and online checks as compact conversation, bounds classifier workspace context, preflights the active model window, preserves current file/tool evidence during UTF-8-safe compaction, and retries one server-reported context overflow with an adaptive smaller prompt and usable output budget.
- Standalone CLI chat and Settings text fields now preserve raw terminal key sequences before Ink normalization, so Backspace, forward Delete, Home, and End work correctly across Linux, macOS, Windows ConPTY, and SSH terminals.
- The standalone CLI now auto-detects reachable local model servers at configured endpoints and bounded llama.cpp loopback ports including 18080, canonicalizes root llama.cpp URLs to /v1, adopts an advertised model only when no explicit model is pinned, and exposes discovery plus probe-port controls in the full Settings page.
- CLI provider health now renders truthful Checking, Ready, Needs setup, and Error states from verified endpoint and model evidence; internal chunk-proof paths stay out of the header, while connection failures and wrong-path HTML responses become short endpoint-specific recovery guidance.
- The standalone terminal chat now uses a bounded redrawable conversation shell with an always-visible absolute working folder, Auto/provider/model status, cursor editing, history, preserved drafts, and transcript paging.
- `rmai settings`, chat `Tab`, and `/settings` now open a selectable full terminal settings page with backend and live model choices, custom model IDs, endpoints, masked API keys, token/context/timeout controls, streaming, workflow limits, folder selection, and global/current-folder scope.
- CLI commands stay bound to the exact invocation or `--project` folder without requiring or searching for a Git repository or `package.json`; symlink and junction escapes are rejected, while per-folder sessions live in user-global CLI storage.
- Greetings and plain exit commands bypass workspace classification, missing-manifest classifier mistakes recover locally, and malformed classifier or Auto action output follows bounded safe repair instead of becoming a false completion.
- Auto flow now executes bounded list/read/search evidence automatically in the displayed folder, pauses interactive writes and shell commands for approval, preapproves folder-confined one-shot writes through `--yes`, and requires a separate `--allow-shell` grant for shell execution.
- Provider/model resolution now honors flow roles, session overrides, provider-specific settings and environment models; provider token, temperature, context, and timeout controls reach the transport, including Ollama output-token limits and timeout zero for slow local warm-up.
- The standalone server CLI now initializes Ink's portable ASM Yoga runtime before rendering and treats `agent` as an alias for the selected provider/backend, so `rmai set-setting agent nvidia` is honored by `rmai chat` on Node.js 18 through 24.
- RealMemory provider tests and normal requests now read the bearer/access token and endpoint saved by Provider Settings from the DB-backed global settings store, while optional SecretStorage login remains supported; missing-credential guidance points to Settings > Providers or the deployment-specific Command Palette login instead of a nonexistent website/menu token flow.
- Authoritative long-message delivery now selects full-inline, verified chunk-ledger, or cumulative summary behavior from the selected provider/model context: missions that safely fit are sent exactly once with character/SHA proof, while larger missions require gap-free ordered pages, cumulative schema/page proof, and complete ledger coverage before any edit, validation, or final claim.
- NVIDIA NIM coder and Direct Model requests now default to automatic streaming for long agent/tool turns and keep all transient retries inside one original request-level timeout budget, so a full 180-second timeout cannot silently restart another 180-second regular request while early disconnects can still recover without losing the mission.
- Provider-neutral streamed requests now publish connecting, accepted, first-chunk, receiving, fallback, and terminal lifecycle updates; a bounded first-response watchdog preserves the original request budget and switches an unopened incremental stream to regular HTTP without replaying or losing the active mission.
- Every provider transport now has a bounded no-header window, accepted streams clear that header watchdog before body delivery, and exhausted network/timeouts pause the exact DB mission at provider_retry so Continue resumes its task packet, attachment cursor, and ownership instead of falsely completing or rebuilding work.
- Accepted incremental streams now use a provider-neutral progress watchdog: every received chunk resets a configurable idle window, active output may continue beyond the base request timeout under a bounded hard ceiling, cancellation retains the response controller, and rescue failures distinguish a stalled selected model from a rescue stream that started but did not finish.
- Host-managed Auto, Planner + Coder, and One Model requests can continue the same assigned task through one bounded compatible rescue model after a no-header stall, with cooldown and handoff provenance persisted; Direct Model remains pinned to its explicitly selected model.
- Runtime mission identity now includes the canonical Auto, Planner + Coder, One Model, or Direct Model flow, so switching flows creates fresh ownership and releases superseded repair-pending agents instead of reusing an incompatible mission.
- Auto, Planner + Coder, and One Model worker transitions now create real SQLite-backed Scheduler, Planner, Inspector, Writer, Validator, Repairer, and Reporter agent/subagent assignments; the VS Code transport badge reads the newest lifecycle event and presents one clean resumable timeout handoff.
- Large Auto and Planner + Coder roadmaps now assign bounded chunks to distinct controller-limited agents and subagents with DB-backed tasks, lifecycle evidence, and a separately owned Validator handoff while executing conflict-safe chunks in order.
- Session rename and delete now use reliable inline persisted controls across VS Code, Chrome, and Edge; Review Changes writes and opens a named .patch artifact in a separate editor tab, Approve materializes and verifies the stored after-snapshot before closing review rows, and Reject/Revert restores the stored before-snapshot.
- The root npm run package workflow now builds synchronized VS Code, Chrome, Edge, and standalone CLI artifacts without registry credentials; version-tagged @reallexi/rmai releases publish only through GitHub Actions using the repository secret named ODIC_TOKEN, with pinned npm 12 install-script approvals, setup-node placeholder replacement, rejection of alternate named npm token variables, and provenance disabled for private-repository compatibility.
- Context and authoritative-attachment ledger limits now default to Auto by selected provider/model across VS Code, Chrome, Edge, and CLI, display their resolved values, and support explicit Custom overrides without allowing stale local num_ctx values to constrain known remote models such as NVIDIA gpt-oss.
- Large prompt and file intake is now lossless and model-context aware across Auto, Planner + Coder, One Model, Direct Model, Chrome, Edge, and CLI @file: every text attachment is read in contiguous hash-verified pages, merged into one bounded requirements ledger, persisted as evidence, and completed before edits, validation, or final claims are accepted.
- Project and session UX now uses a professional responsive home across VS Code, Chrome, and Edge, with searchable recent work, overview metrics, intentional empty/loading states, and session-owned Opening, Working, Queued, Selected, Ready, and Needs-attention indicators so active work is never ambiguous.
- SQLite session, roadmap, plan, review, and browser-run CRUD now uses targeted writes/deletes with fallback mutation reconciliation, upgrade-safe session migration, complete deletion cascades, typed settings, and a standalone zero-runtime-dependency @reallexi/rmai npm package that installs globally and runs without any extension.
- Failed validation output now returns to the same coder model for one strict schema repair, supports unique targeted replacements and safe full-file fixes across common languages, records real pre-write review snapshots, rejects ambiguous or wrong-root mutations, and directly repairs the observed malformed Python typing import before rerunning validation.
- Promoted long product specifications now bypass the legacy read-only image-chat diagnostic after create/update/repair intent is established; image, media, response, issue, and check vocabulary in unrelated specification sections can no longer stop Writer before file creation.
- Long uploaded specifications now remain the authoritative mission through Send, delayed approval, extension reload, planning, Writer execution, and validation; NVIDIA gpt-oss uses its real context window, while genuinely undersized contexts stop explicitly instead of silently sampling away deliverables.
- Documentation-only workspaces are treated as empty implementations even when projects.md mentions React, Vite, Python, or an older stack; fresh requests clear prior roadmap, task, file, validation, chat-history, and abandoned Continue ownership before new project work begins.
- Approval execution now preserves run-token ownership and always releases its lifecycle guard, persisted attachments are bound to the pending request, auxiliary logs no longer relabel a primary product specification, and explicit command-only attachments retain command approval.
- Active DB-backed work now outranks stale message-level failed/blocked statuses and non-streaming assistant rows; Working clears only for an explicit final/terminal event or when queue, TODO, and run evidence are genuinely inactive.
- Warnings and errors now use one compact collapsed result disclosure across VS Code, Chrome, Edge, and web; recoverable alerts preserve the live Agent working state until an explicit terminal event, while expanded details show DB-backed agents, subagents, work evidence, and changed files or artifacts.
- Direct Model validation repair now uses bounded coder-role exchanges containing the exact persisted failure and current model-owned task packet, rejects premature command reruns, counts every rejected no-progress response consistently, reruns the failed checkpoint after a real mutation, and avoids replaying a growing full conversation.
- Direct Model validation now recovers from stale visible terminals within a short launch bound, owns and terminates child process trees, preserves structured timeout evidence, checkpoints multi-step validation, keeps inconclusive runs repair-pending without a completion timestamp, and makes Continue resume the exact unfinished captured-runner step before any new model request while restoring selected-model story, task, subtask, agent, and subagent ownership.
- Direct Model empty-project recovery now rejects dependency-open file batches before workspace mutation, retains closure-safe candidate slices for bounded missing-file repairs, enforces explicitly requested SQLite evidence, repairs verified missing relative stylesheets before any new inspection call, and never treats pending or truncated execution.fileChanges JSON as a completed chat response.
- The VS Code header now stays on one compact stacking layer, keeps Settings directly available on narrow panels, exposes every More-menu page inside a bounded viewport, preserves external support-link navigation, closes popups only from the true backdrop, and reports expanded state for accessible menu and settings controls.
- Auto, One Model, Planner + Coder, and Direct Model prompts now begin from the same principal-engineering organization contract, with auditable security, vertical-slice delivery, real validation, and no fabricated completion claims across editor, browser, web, backend, CLI, and MCP surfaces.
- First-install setup is serialized before readiness checks, every readiness line has one consistent prefix, identity logging is deduplicated, review decisions are idempotent by review ID and status, and the reusable quick tutorial plus confirmed Git/GitHub CLI workflow make new clients development-ready without silent system changes.
- VS Code, Chrome, Edge, and the web companion now use one conversation-first layout: the transcript is the only default scroll surface, live work stays in one compact non-chat strip, operational details remain collapsed until requested, and the bounded inline composer expands only for real input.
- Changed-file review now uses one bounded responsive sheet with a compact action hierarchy, horizontal file selector, wrapped diff, no nested outer scrolling, and screenshot geometry gates at the reported 531-pixel width and a short viewport.
- Terminal lifecycle reconciliation now preserves completed intake/development evidence, marks real validation failures, blocks review/sign-off, filters legacy duplicate tasks and synthetic pass rows, refreshes the TODO immediately after DB finalization, and clears stale Agent working cards.
- Generated multi-package projects now require coherent semantic script graphs before validation: delegated script names, package owners, local runner paths, bounded tests, literal workspaces, database write/read proof, and frontend/API integration evidence remain aligned across live Writer batches and evidence-scoped repairs.
- Active missions now render the exact DB-backed request, story, task, subtask, owner, assigned agents/subagents, validation, screenshot, proof, sign-off, and ordered TODO state across VS Code, Chrome, and Edge; incomplete packets show Preparing instead of a false Working-with-zero-agents state.
- The packaged MCP server now executes bounded workspace list/read/write/replace/search, approved command, setup, memory, queue, handoff, proof, project-summary, and web-search tools with schemas, workspace/path guards, DB audit rows, and normalized repair evidence instead of advertising placeholders.
- Packaged runtime preflight now uses ESM-safe process APIs, large-request preplanning avoids stale unowned provider calls, and generated package test-proof JavaScript is base64-wrapped so Windows and Node 24 cannot corrupt validation commands.
- Chrome and Edge now use a rebuilt responsive side-panel with a static brand header, one compact browser target, closed workflow/queue/review/support disclosures, accessible controls, no default nested scrollers, and verified 300/380/500-pixel geometry.
- Chrome and Edge startup now has a bounded background-service timeout and recoverable setup shell, while Chromium validation rejects unresolved loading-only splash screens instead of accepting them as UI proof.
- Chrome and Edge research-to-email workflows now use typed approval records, exact new-Compose transactions isolated from stale drafts, strong target matching, single-fire clicks, delayed polling, explicit Message sent proof, and exact sent-body responses.
- Failed browser, VS Code, and web-companion runs now prepare structural-only GitHub-ready support drafts for a fixed trusted repository, with explicit user review/submission, local Markdown/JSON fallbacks, and no raw prompts, page content, email fields, credentials, provider secrets, or private URLs.
- VS Code failure reporting now renders normalized DB-backed diagnosis cards: warnings remain warnings, timeouts remain command-state events, test failures stay distinct, and raw terminal output is available only inside collapsed technical details.
- UI screenshot proof is verified, persisted, and attached as soon as Chromium captures it, so later build/test failures cannot replace real proof with a false missing-screenshot or placeholder diagnosis; CommonJS smoke scripts with ESM syntax receive a bounded deterministic repair.
- Functionality and design completion now requires the full MCP validation suite: doctor, tool discovery, queue inspection, handoff export, and SDLC proof export are recorded as separate evidence-backed phases, with compact proof summaries keeping terminal logs bounded.
- Validation can repair undeclared Node dependencies in the nearest owning package, while approval commits ignore stale missing review paths and scope commits to the reviewed staged files.
- Root, browser, backend, and web package trees now pass npm audit with zero known advisories after updating development test/build transitive dependencies.
- VS Code validation, screenshots, shell temp scripts, tool output, chat-memory context, review artifacts, MCP scaffold files, models, logs, downloads, and testing sandbox now default to VS Code global storage; legacy .real-memory-agent paths are remapped away from client workspaces.
- Real Memory Agile workflow control is now configurable from VS Code, Chrome, and Edge settings with system-load profile, max agents, max subagents per agent, screenshot proof, Playwright proof, and changed-file SDLC attachment requirements.
- The 1.2.0 SDLC foundation now reads as a clean cross-surface product story: requests can be planned, validated, reviewed, evidenced, and signed off across editor, browser, backend, and MCP workflows.
- Model/provider readiness checks, compact context handling, chunked execution, and lightweight local-model settings help reduce wasted calls and keep large requests manageable.
- Workspace and browser flows now treat empty product workspaces, empty folders, and no-match filename/path searches as terminal evidence; create/build requests move to real file creation instead of repeating inspection.
- Direct Model empty-workspace recovery now asks the selected model for a complete model-owned action schema packet with ordered TODOs, executable file changes, and validation proof before applying generated project files.
- Direct Model now accepts model-filled file changes from action schema packets, tool-plan arguments, and inline write syntax, then performs one strict selected-model schema repair before any empty-workspace blocker.
- Runtime state records now use the active VS Code workspace identity so sessions, queues, evidence, and sign-off records stay attached to the correct project.
- VS Code chat now keeps progress, tool, TODO, model, and status details out of chat history; concise live status renders in the status strip while detailed diagnostics stay in the Activity Log.
- The VS Code Agent Thinking strip is now a full-width responsive disclosure docked above the composer, with mouse and keyboard expansion, bounded detail scrolling, and narrow-panel geometry that keeps status content clear of the input footer.
- VS Code and browser extension message, Activity Log, Files, workflow status, and composer panels now use a cleaner bounded responsive shell so the input and Send controls stay visible in narrow editor and side-panel layouts.
- Changed files, validation records, review outcomes, screenshots, artifacts, and screenshot N/A reasons can be tied to the active story, task, and subtask before completion.
- README and release notes now use high-level public language for users, marketplace visitors, agencies, enterprise buyers, and investors without exposing private implementation details.
- Routine follow-up releases on this line should use patch version bumps while keeping VS Code, MCP, Chrome, Edge, package lockfiles, and public changelog copies synchronized.

## Build Contract

- This file is generated by `scripts/sync-extension-changelog.cjs` during extension build/package/media flows.
- Keep release notes clean and model-shareable: one concise fact per bullet, no stale debugging noise, no repeated placeholder sections.
- When a build changes research, browser automation, packaging, validation, media, or public-store behavior, update this script or pass `RM_AI_CHANGELOG_NOTE` with the release note before building.
- The same content must exist in the repository root, the browser extension folder, and the public media bundle.
