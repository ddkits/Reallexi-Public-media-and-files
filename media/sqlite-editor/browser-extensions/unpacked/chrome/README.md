# Reallexi SQLite Editor for Chrome

This is the unpacked Manifest V3 browser extension generated by `npm run package`.

It opens local SQLite database files in a packaged extension page and does not request host permissions, tab-reading permissions, content-script injection, or remote code.
